{
  "name": "oaes-deployment",
  "version": "1.0.0",
  "description": "Production scripts for Render",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}